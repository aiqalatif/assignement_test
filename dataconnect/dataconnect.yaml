specVersion: "v1beta"
serviceId: "assignment_test"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "assignment_test-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
